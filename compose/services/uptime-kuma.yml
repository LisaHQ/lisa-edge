services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: lisa-uptime
    restart: unless-stopped
    ports:
      - "${UPTIME_KUMA_BIND_ADDR:-127.0.0.1}:${UPTIME_KUMA_PORT:-3001}:3001"
    volumes:
      - ${DATA_ROOT:-/srv/lisa-edge}/docker/volumes/uptime-kuma:/app/data
